## Generated by Panasa Agent Builder — provider requirements
## Agent: {{ agent_id }}  Version: {{ version }}
## Required for `terraform validate` (CLAUDE.md Section 6 IaC validation
## suite) to resolve the AWS provider schema — every other module in this
## generated configuration assumes this file is always present (base is an
## always-on module, conditional.py's _ALWAYS_ON_MODULES).

terraform {
  required_version = ">= 1.5.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}
