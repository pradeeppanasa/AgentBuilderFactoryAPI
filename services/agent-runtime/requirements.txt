# Generic Agent Runtime — a separate deployable from the Factory Runtime
# (F8/R10: zero shared code, its own pinned versions). Only what main.py's
# import graph actually uses; no langgraph/opentelemetry/langchain-aws —
# not imported anywhere in this service, so not listed here (R20's
# "conditional, only what's needed" spirit applies to dependencies too).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
boto3==1.35.90
litellm==1.96.2
structlog==24.4.0
# Sprint 3 Phase 9 (S-03, R70) — quota.py's rpm/rpd rate-limit counters.
redis==5.2.1
# Sprint 4 Phase 4 (S-12) — JwtAuthProvider's JWKS/RS256 verification.
# Same pinned version as the Factory Runtime's OWN (unrelated) HS256
# Console-user auth (requirements.txt) purely for consistency — the two
# never share code or a trust root (see auth.py's JwtAuthProvider
# docstring). [crypto] extra pulls in `cryptography`, needed for RS256.
pyjwt[crypto]==2.10.1
